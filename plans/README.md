# Implementation Plans

Generated by the improve skill on 2026-06-14 and extended on 2026-07-08.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

These plans are scoped to creating a clean shared/platform base before splitting
`attraction` and `leadgen` into separate repositories, and before publishing the
project as fork-friendly open source. They intentionally do not try to fix every
architectural issue found in the deep review.

## Execution Order & Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| [001](./001-record-product-split-decision.md) | Record the product split decision | P1 | S | - | DONE |
| [002](./002-split-sqlite-repository-roles.md) | Split the SQLite repository interface by role | P1 | M | 001 | DONE |
| [003](./003-extract-platform-and-module-route-registrars.md) | Extract platform and module route registrars | P1 | L | 001, 002 | DONE |
| [004](./004-define-module-capability-manifest-and-agent-data-boundary.md) | Define module capability manifest and agent data boundary | P1 | M | 001, 002, 003 | DONE |
| [005](./005-define-fork-owned-module-extension-seam.md) | Define fork-owned module extension seam | P1 | M | 001, 002, 003, 004 | DONE |
| [006](./006-define-manager-call-attribution-policy.md) | Define manager call attribution policy for shared attraction roles | P1 | M | manager catalog includes 7538, 118 | DONE |
| [007](./007-unify-deal-timeline-card.md) | Unify the deal timeline card with all events and sale cost economics | P1 | L | - | DONE |
| [008](./008-capture-deal-meeting-slots.md) | Capture deal meeting slots 1-3 in sync and storage | P1 | L | - | DONE |
| [009](./009-count-meeting-slots-in-reports-and-economics.md) | Count meeting slots in reports, timelines, and economics | P1 | L | 008 | DONE |
| [010](./010-display-meeting-slots-in-dashboard-ui.md) | Display meeting slots 1-3 in dashboard deal cards | P1 | M | 008, 009 | DONE |
| [011](./011-complete-agent-readable-report-catalog.md) | Complete agent-readable attraction report catalog | P1 | M | 004 | DONE |
| [012](./012-build-read-only-attraction-agent-gateway.md) | Build read-only attraction agent gateway | P1 | M | 011 | DONE |
| [013](./013-add-stdio-mcp-server-and-agent-evals.md) | Add stdio MCP server and agent evals | P1 | M | 011, 012 | DONE |
| [014](./014-fix-workload-heatmap-metric-contract.md) | Make workload heatmaps use the same metric contract as their table rows | P1 | M | - | DONE |
| [015](./015-record-call-enrichment-security-contract.md) | Record the call enrichment writeback security contract | P1 | S | - | DONE |
| [016](./016-add-call-enrichment-field-allowlist.md) | Add the call enrichment field allowlist and value contracts | P1 | M | 015 | DONE |
| [017](./017-add-call-enrichment-proposal-storage.md) | Add call enrichment proposal storage, lifecycle, and audit events | P1 | M | 016 | DONE |
| [018](./018-add-bitrix-call-event-intake.md) | Add idempotent Bitrix call event intake for automatic analysis | P1 | M | 017 | DONE |
| [019](./019-add-cheap-dialogue-gate.md) | Add a cheap OpenRouter dialogue gate before full call analysis | P1 | M | 018 | DONE |
| [020](./020-wire-automatic-call-analysis-enrichment-orchestrator.md) | Wire automatic call analysis into an enrichment orchestrator | P1 | M | 017, 018, 019 | DONE |
| [021](./021-add-enrichment-extraction-agent.md) | Add the CRM enrichment extraction agent | P1 | M | 016, 020 | DONE |
| [022](./022-add-current-crm-values-diff.md) | Add current CRM value reads and meaningful enrichment diff | P1 | L | 016, 021 | DONE |
| [023](./023-add-telegram-enrichment-approval-flow.md) | Add Telegram approval batches for enrichment proposals | P1 | L | 017, 022 | DONE |
| [024](./024-add-narrow-bitrix-enrichment-write-adapter.md) | Add the narrow Bitrix enrichment write adapter | P1 | L | 015, 016, 022, 023 | DONE |
| [025](./025-add-call-enrichment-rollout-and-e2e-verification.md) | Add rollout gates and end-to-end verification for call enrichment | P1 | M | 020, 021, 022, 023, 024 | DONE |
| [026](./026-operational-threshold-settings.md) | Add editable operational threshold settings | P1 | M | - | DONE |
| [027](./027-operational-dashboard-report-api.md) | Build the operational dashboard report | P1 | L | 026 | DONE |
| [028](./028-operational-dashboard-scene.md) | Render the «Операционный» dashboard scene | P1 | L | 027 | DONE |
| [029](./029-add-fact-conversion-diagnostics.md) | Add fact-step conversion diagnostics to the conversion report | P1 | M | - | DONE |
| [030](./030-add-speed-sla-buckets.md) | Add speed and SLA buckets to the conversion trajectory | P1 | M | 029 | DONE |
| [031](./031-add-manager-diagnostic-cards.md) | Add manager diagnostic cards for conversion trajectory gaps | P1 | M | 029, 030 | DONE |
| [032](./032-add-source-customer-loss-shapes.md) | Add source and customer loss-shape analysis | P1 | M | 029, 030 | DONE |
| [033](./033-fix-conversion-trajectory-review-findings.md) | Fix conversion trajectory review findings | P0 | M | 029, 030, 031, 032 | DONE |
| [034](./034-simplify-conversion-report.md) | Simplify the conversion report around decisions and event outcomes | P0 | L | 029, 030, 031, 032, 033 | DONE |
| [035](./035-fix-conversion-event-correctness.md) | Fix conversion event correctness | P0 | S | 034 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale - finding fixed independently or approach
abandoned).

## Dependency Notes

- 001 must land first because it records the decision every shared/platform
  follow-up must obey: `attraction` and `leadgen` are separate products, not two
  skins of one report.
- 002 should land before 003 because route registrars should depend on narrow
  storage roles, not on the full `SqliteRepository` adapter.
- 003 is the first code split that makes module disablement real at the HTTP
  interface without physically forking the repository yet.
- 004 should land after 002 and 003 so future modules and AI-agent consumers use
  explicit storage/route seams instead of direct repository or report internals.
- 005 should land after 004 because the manifest must exist before fork-owned
  modules can bring their own manifest adapter, live route availability, source
  data, and integration rules. It keeps the project source-code-fork friendly
  without introducing a package/plugin runtime yet.
- 006 is independent of the platform split plans, but depends on the active
  attraction manager catalog containing `7538` and `118`. It should land after
  the manager-addition branch or absorb that change explicitly.
- 007 is independent of the platform split plans. It should be implemented on a
  fresh `codex/unified-deal-timeline-card` branch and must keep the deal card
  scoped to local API/SQLite data, safe attraction fields and the existing
  dashboard design system. Execution completed on
  `codex/unified-deal-timeline-card`; blocks A-E are done, including focused
  suites, workspace checks, browser smoke and final CRG/code-review gate.
- 008 starts the multi-meeting-slot sequence. It must land before report or UI
  work because the current sync/storage shape can represent only one deal
  meeting field pair.
- 009 depends on 008 because meeting counts, lifecycle events, workload rows and
  economics must consume one canonical `meetingSlots` model and avoid double
  counting CRM meeting activities.
- 010 depends on 008 and 009 because the dashboard should render the final API
  shape instead of inventing client-side meeting counts.
- 011 depends on the module capability boundary from 004. It must complete the
  attraction report catalog before an agent gateway can safely execute reports.
- 012 depends on 011 because the gateway must use manifest report descriptors
  and `agentReadable` policy rather than maintaining a hidden allowlist.
- 013 depends on 011 and 012 because MCP should be a thin stdio protocol adapter
  over reviewed read-only gateway methods.
- 014 is independent of the platform and MCP plans. It must land before the
  calls/tasks/meetings heatmap branch is considered review-ready because the
  current expanded cards mix table-row metrics with different heatmap
  populations and off-grid counts.
- 015 starts the call enrichment feature sequence. It must land before code
  because the current repository security contract is read-only and explicitly
  forbids generic Bitrix writes.
- 016 depends on 015 because the allowlist and entity routing must implement the
  security decision, not invent a second contract in code. Later prompts,
  Telegram messages, diff logic, and Bitrix writeback must import this contract
  rather than duplicating field codes.
- 017 depends on 016 because proposal rows must store only approved field/entity
  references. It creates the lifecycle and audit substrate before intake,
  extraction, Telegram, or writeback.
- 018 depends on 017 so repeated Bitrix call events can be acknowledged
  idempotently and have a safe downstream state target.
- 019 depends on 018 because the cheap dialogue gate is used by the automatic
  intake path. It must run before full analysis to skip voicemail, robot,
  silence, and no-answer calls.
- 020 depends on 017, 018, and 019 because it owns the automatic state machine:
  intake -> dialogue gate -> full analysis -> enrichment hook. It must not send
  Telegram or write Bitrix directly.
- 021 depends on 016 and 020 because extraction must use the central field
  contract and run only after automatic analysis has produced transcript data.
- 022 depends on 016 and 021 because current-value reads and diff decisions need
  both the approved field descriptors and extraction candidates.
- 023 depends on 017 and 022 because Telegram should send only stored,
  meaningful proposals. It must not contain diff logic or Bitrix write logic.
- 024 depends on 015, 016, 022, and 023 because writeback is destructive and must
  apply only manager-approved, unexpired, allowlisted proposals after a fresh
  current-value check.
- 025 depends on the full feature chain because rollout modes and end-to-end
  tests prove dry-run, Telegram-only, limited-write, full V1, no-dialogue skip,
  no-op skip, deal/contact routing, and 48-hour expiry behavior.
- 026 starts the operational dashboard sequence. It must land first because the
  dashboard risk rules and first-touch SLA panel need one editable source of
  truth for stage-aging, hygiene, and SLA thresholds.
- 027 depends on 026 because the report must read thresholds from local settings
  instead of hardcoding risk limits. Meeting aggregation in 027 is strictly by
  meeting slot index: «Встреча 1», «Встреча 2», «Встреча 3». `typeValue` is
  contextual metadata only and must not become a report grouping dimension.
- 028 depends on 027 because the UI must render the reviewed report contract
  from `/api/reports/operational-dashboard`; it must not compute dashboard
  metrics in the browser.
- 029 starts the conversion trajectory deepening sequence. It lands first
  because all later management views need one canonical set of fact steps and
  conversion gaps: successful call, CRM meeting stage, factual meeting, event,
  contract, and sale.
- 030 depends on 029 because speed/SLA buckets must use the same fact-step
  definitions and must keep current open-stage aging separate from historical
  median stage duration.
- 031 depends on 029 and 030 because manager diagnostics should consume the
  canonical gap counts and slow/stale counts instead of inventing UI-only
  interpretations.
- 032 depends on 029 and 030 because source/customer loss-shape analysis should
  reuse the same gap and timing semantics, including the rule that basket,
  return and other loss stages are terminal outcomes, not active "on stage" WIP.

## Operational Dashboard Execution Goal

Goal: implement the operational dashboard in this order:
[026 operational threshold settings](./026-operational-threshold-settings.md)
→ [027 operational dashboard report API](./027-operational-dashboard-report-api.md)
→ [028 operational dashboard scene](./028-operational-dashboard-scene.md).

Execution loop for each plan:

1. Before implementation: run the session preflight, read the target plan, run
   CRG/source lookup for the touched module, run an architecture/codebase pass
   over the module/interface/seam shape, run a Ponytail scope check, then run a
   code-review risk checklist before editing.
2. Implement only the current plan's in-scope files. Keep meeting counts by
   `Встреча 1/2/3`; do not group meetings by `typeValue`.
3. After implementation: run targeted tests, then review the diff with
   code-reviewer, Ponytail, improve deep, and architecture/codebase lenses.
   Fix confirmed findings before starting the next plan.
4. After 028: run API tests, web tests, `pnpm typecheck`, `pnpm lint`, and
   browser verification against the operational dashboard UI.

## Conversion Trajectory Execution Goal

Goal: deepen the existing "Конверсии" report in this order:
[029 fact conversion diagnostics](./029-add-fact-conversion-diagnostics.md)
→ [030 speed and SLA buckets](./030-add-speed-sla-buckets.md)
→ [031 manager diagnostic cards](./031-add-manager-diagnostic-cards.md)
→ [032 source and customer loss shapes](./032-add-source-customer-loss-shapes.md).

Execution profile: [conversion-trajectory-execution-profile](./conversion-trajectory-execution-profile.md).

Execution loop for each plan:

1. Run the session preflight, then read the plan fully before editing.
2. Keep the existing report tab and endpoint: "Конверсии" and
   `/api/reports/source-cohort-conversion`.
3. Put business-truth calculations in the domain report builder and contracts,
   not in ad hoc browser code.
4. After every plan: run targeted tests, code-reviewer, Ponytail, and
   architecture/improve review; fix confirmed findings; rerun tests and review
   until no blocking findings remain.
5. Verify the result in the in-app Browser against the local API values before
   moving to the next plan.
6. Skip the broader data-quality layer for this sequence; it is intentionally
   out of scope until the operator asks for it.

## Verification Baseline

Use the repo commands from `AGENTS.md`:

- `pnpm session:preflight`
- `pnpm ontology:validate`
- `pnpm --filter @bitrix24-reporting/api typecheck`
- `pnpm --filter @bitrix24-reporting/api exec vitest run test/http.test.ts`
- `pnpm --filter @bitrix24-reporting/api exec vitest run test/sqlite.test.ts`
- `pnpm typecheck`
- `pnpm lint`

During the advisory pass, `pnpm --filter @bitrix24-reporting/api typecheck` and
`pnpm --filter @bitrix24-reporting/contracts typecheck` passed at commit
`5a2884f`. `pnpm audit --prod` reported one moderate transitive advisory in
`qs` via `express`; that is not part of these platform-foundation plans.

## Findings Considered And Rejected

- Full repository fork now: rejected for this sequence because it would copy the
  current `createApp`, `SqliteRepository`, and `ProtoApp` coupling into two
  repositories.
- Full web runtime refactor now: deferred. `docs/architecture/web-runtime.md`
  requires `ProtoApp` to remain the one supported runtime interface; internal
  web adapters can come after server/storage seams.
- Full contracts package split now: deferred. It is useful, but less blocking
  than storage roles and route ownership for safe repository extraction.
- Package/plugin runtime now: deferred. Open-source forkability should first use
  a source-level extension seam; dynamic package loading can come later if real
  fork maintainers need it.
